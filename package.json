{
  "name": "ts-site",
  "version": "0.1.0",
  "description": "Deploy private static sites to a Tailscale host",
  "bin": {
    "ts-site": "src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "host": "node src/host.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
