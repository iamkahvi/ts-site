# Copy this file to .env on a client. Bun loads .env from the current working
# directory; variables already exported by the shell take precedence.

TS_SITE_HOST_URL=http://m900:8080
# Must match TS_SITE_API_TOKEN on the host.
TS_SITE_API_TOKEN=
